export enum DriverStatus {
    ONLINE = "ONLINE",
    OFFLINE = "OFFLINE",
    BUSY = "BUSY"
}

export enum BookingStatus {
    PENDING = "PENDING",
    ACCEPTED = "ACCEPTED",
    STARTING = "STARTING",
    CANCELED = "CANCELED",
    COMPLETED = "COMPLETED",
    REJECTED = "REJECTED"
}